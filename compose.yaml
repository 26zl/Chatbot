services:
  rag-docs-bot:
    build: .
    image: rag-docs-bot
    ports:
      - "127.0.0.1:8501:8501"
    volumes:
      - ./data:/app/data
      - ./.streamlit:/app/.streamlit:ro
    restart: unless-stopped
    
